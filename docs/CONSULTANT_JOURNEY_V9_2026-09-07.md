# Fresh consultant development journey v9

After the sponsor-applicability, PDF pagination and cover-letter changes, the
actual `--scenario-set all --allow-model-calls` probe ran against source commit
`f76a06b4dc24602d8e21540274fded1dde0691a9`. The new, non-overwritten report is
`eval_output/consultant_journey_2026-09-07-v9.json`.

Observed: **22/22 checks passed**, 22 extraction calls, **124,392 reported tokens**,
zero retries, zero mailbox calls and zero real documents. Each turn retains a raw
provider response and one usage record. The requested model was
`deepseek-v4-flash`. The source-manifest v2 check binds the report to the current
implementation, probe, policy and dependencies; the same development scenarios
and fixed evaluation clock are retained. This is not a new blind holdout or a
live official-policy verification.

The four conversations cover student evidence obstacles and changing funder,
self-employment/funds/pausing, and Chinese/English durable pacing. All 22 captured
replies were read. They retain undecided dates and supplied identity, give the
application entry point and practical next steps, distinguish own-account
transfers from income, refuse a sufficiency guarantee, and honor a pause. Remaining
editorial weaknesses include the generic personal-sponsor introduction and the
English comma-joined passport-name/birthday receipt. No independent recipient
naturalness score is claimed.

The current-report test now references v9 without changing its source checks or
rewriting v8. The complete consultant replay module passed **46 tests in 2.01
seconds**. The full-suite run already in progress had collected the old v8
reference; its historical failure is not erased by this later test update.
Financial-report freshness remains unresolved until a separate new run is checked.
Gmail deployment, applicant consent and final recipient delivery are unchanged.
