# Gmail 旅游材料全流程记录（进行中）

此记录是获授权的虚构案例验证，不是独立用户验收或真实证件审验。
邮件正文没有测试标记或授权码；PDF 样本明确标为虚构，不可用于申请。

## 当前实际运行

实际服务已统一为一个 LaunchAgent，使用 `--accept-new-senders`，无指定主题，
采用直接咨询模式。2026-09-07 13:28 后检查间隔由 60 秒缩短至 30 秒。
原限定邮箱进程及单独演示进程均已停止；原实际客户档案保留。
开放入口已收到原先不在该服务单发件人范围内的邮箱，并实际发送回复。

## 已观察到的流程

1. 普通邮件询问英国旅游材料，未提供固定格式。Gmail 客户收件端收到准备说明。
2. 客户补充香港申请、姓名、生日、地址、收入、资金安排、具体出行日期和历史声明，
   并经 Gmail 网页上传五份 PDF：护照信息样本、在读证明、居留样本、行程、银行流水。
3. 六份样本（包括保留的更正版）均已逐页渲染检查。首轮只发送五份，银行户名为
   Lin Chan，与护照 Lin Chen 不同；更正版尚未发送，不能提前声称纠错验证成功。
4. 13:34 实际回信已经逐份确认收到，提出护照扫描/照片需求并保持其他文件待核对。
   没有资料处理页脚或回复授权码要求。

## 当前失败和边界

- 系统识别护照信息页为样本，不能把它当成有效护照。这是正常真实性边界，不应去掉。
- 居留样本被标为需复核；在读证明、行程和银行流水未完成分类。
  银行户名差异尚未被作为具体问题反馈，不能声称纠错用例已通过。
- 回复仍出现“日期还没定不影响先做这一项”，但客户已明确提供 2026-11-10 至 2026-11-17。
  需要核对档案状态与建议文案，避免残留话术。
- 没有手工接受文档、伪造客户确认或人工复核，没有最终 ZIP 生成/投递成功记录。
- 下一步需要让演示样本验证与真实材料审验明确分离，同时诊断普通文档分类和日期话术。
  不以关闭真实客户的文档门禁换取演示通过。

样本生成器：`scripts/gmail_tourism_documents.py`。输入目录：
`output/pdf/gmail-tourism-20260907/`。原始失败保留，不覆盖或删除邮件。

## 后续诊断

日期已在档案中正确保存为 2026-11-10 至 2026-11-17，待定字段已清空；
问题是学生材料建议中的固定文案。现已按日期是否齐全决定是否显示“日期还没定”，
并增加中英文回归。读取器现在用固定安全错误码区分格式、超时、证据校验及其他失败，
不把异常消息或原文泄露给客户。

对三份虚构普通文档分别做了两轮真实 DeepSeek 诊断，共六次调用、14,108 tokens，
没有新邮件发送、附件改写或文档接受操作。两轮均返回结构化内容；失败并非全部属于
无法分类。第一轮学生津贴被误提议为个人资助和工资，行程日期被映射成邀请活动日期。
提取说明已进一步约束这些语义。第二轮这类错误减少，但学生证明和行程仍出现
低置信度的额外资金字段，因此未通过校验。

银行流水明确写了 `Statement period: 1 August 2026 to 31 August 2026`，旧日期角色
校验只认单个结单日期标签。现支持完整、有序、明确的结单区间，且只能选结束日期，
不能把开始日期、旅行日期、不完整日期或多选日期当结单日期。第二轮真实调用成功
保留 Lin Chan、GBP 12,500.00 和 2026-08-31；这不验证银行真实性或资金充足性。

相关选择测试 54 项通过、Ruff/mypy 通过。完整当前模型评测和材料最终交付仍未完成；
旧报告的源码指纹已过期，不作为当前版本通过的证据。

## 13:46–13:50 留存附件恢复与真实追问

通过 `review_document.py retry` 对原始银行流水、在读证明、行程各做一次真实模型重读。
三次均恢复为已读取材料，保留同一原始事件、文件哈希和审计关联；没有手工批准文件、
伪造客户事件或发送材料包。护照及居留样本的限制没有取消。
银行户名 Lin Chan 与档案 Lin Chen 的冲突现在由一致性检查发现，资金持有人检查也保持阻断。

修复了重读命令错误地把 service_request 与 explicit_consent 的配置指纹混为一谈的问题。
注册模型不同、已撤回处理、未授予的显式授权模式仍禁止云端重读；相关集成测试通过。
同时修复 `pytest` 可执行入口无法导入跨测试辅助模块的问题，配置项目根目录的测试路径。

13:49 从 Gmail 网页发送普通追问：已发送在读证明、行程和流水，希望知道具体哪项有问题，
不想把全部材料再准备一遍。13:50 客户收件端实际收到回信，已明确列出 Lin Chan / Lin Chen
和 bank_statement_original.pdf 的持有人问题，没有再把学生证明及行程列为人工核对。

该回信仍存在新的失败：把“不想把全部材料再准备一遍”误判为要求完整准备清单，
在具体问题前附上过长的通用框架，并再次建议申请已经收到的在读证明。
这条真实会话不算自然度验收通过。后续应修复否定意图识别和已收材料的下一步建议。
居留样本有效性提示的新版措辞已补充，但尚未在这封历史回信中生效。

全量回归本轮仅剩两项旧模型报告源码指纹不一致的失败（不是当前模型通过证据）。
新财务评测 v23 和多轮会话评测 v15 已启动，结果必须单独检查。
更正流水仍未发送，最终 ZIP 未生成或投递，全流程交付未完成。

## 13:54 更正邮件与后续回归

之后修复了“不想／不打算”被当成完整清单请求的否定识别，并为真实追问原句添加回归。
已读取的在读证明也不再触发“先向学校申请”的下一步建议；失效或被替代的证明不冒充已收有效材料。
相关选择测试 98 项通过，Ruff 和 mypy 通过。

v15 会话评测 22/22、v23 财务评测 4/4 真实调用通过，但早于上述两处修复，只是历史证据。
当前源码的 v16 会话与 v24 财务评测已启动，不能提前声称结果。

13:54 经 Gmail 网页实际发送更正流水及替换说明，页面已显示已发送附件。
说明明确用 bank_statement_corrected.pdf 替换 bank_statement_original.pdf，户名应为 Lin Chen，
账户、结单期间和金额不变，属于申请人本人。附件仍显著标记为虚构样本。
尚需检查服务端如何关联这次更正、客户收到的实际答复以及残余门禁；没有最终 ZIP。

13:55 服务端已读取更正流水，但未将旧版标为被替代，姓名与旧流水持有人冲突仍在。
当前附件入库逻辑只对会议邀请函设置自动替代关系，未处理客户明确指定的银行流水替换。
这是实际发现的流程缺口，不应通过手动清除冲突掩盖。下一步需要按客户明确指令、
同案唯一目标、可靠的新证据建立替换关联，同时保留原件及来源，并测试否定、歧义、
不同账户与无效新材料不能误替换。当前 9 份入库记录包含 3 次原件重读记录，不是 9 份不同文件。

## 14:01 同账户更正替换已通过真实邮件验证

新增确定性的客户流水更正关联：只接受当前客户在同一线程明确指定的一组新旧文件，
核对已读取的新旧流水属于相同账户、结单日期、币种、金额和文档类型，且新版户名对应档案。
保留旧文件、原读取记录和来源；仅把被指定的旧版及其证据退出当前核对，记录客户指令邮件 ID。
否定、条件句、引用、歧义文件名、不同账户、不可靠新版或跨客户指令均不直接替换。
Gmail 自动插入的文件名前换行已加入回归，不要求客户手工改成固定行格式。

只读演练未写入数据库。随后通过 Gmail 网页发送新的明确替换指令，服务正常处理该邮件
`1a07a75088c9ec16`，自动把 doc-98778b98490e 标为 SUPERSEDED，
doc-3c6991871435 关联为替代版本。实际档案只剩居留和护照样本的两个限制；
姓名和旧流水持有人冲突已消除。没有手工清除问题、删除原件或伪造审核。
14:01 客户收件端回信也仅列居留／护照问题，没有再次要求纠正 Lin Chan。
Gmail 将重复文本折叠，已展开查看完整正文。

已补充仅在替换完成且有审计证据时告知客户“已改用新版、旧版保留”的回执，
该回执选择测试已通过，但不冒称出现在上述历史回信中。
本轮全量测试 5,556 通过、2 项旧报告源码指纹失败；之后回执相关选择测试 120 通过。
v16 会话 22/22、v24 财务 4/4 实际评测均完成，但早于流水替换修复，只作为历史证据。
需在发布候选版本稳定后刷新评测，不对每次小改动重复付费调用来冒充持续验收。

最终材料 ZIP 和完整流程录像仍未完成；样本限制没有解除或伪装成真实证件审核通过。
