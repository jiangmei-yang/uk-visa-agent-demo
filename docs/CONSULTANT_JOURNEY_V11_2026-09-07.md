# Current-source consultant journey v11

At source `c6fa15b`, the actual four-journey DeepSeek probe made **22 calls** with
**22 passing existing turn checks**, **124,382 reported tokens**, zero retries,
zero mailbox calls and no real documents. The output is retained separately as
`eval_output/consultant_journey_2026-09-07-v11.json`; no earlier evidence changed.

All 22 complete replies were read. The income-evidence turn now gives the
reviewed self-employment guidance without the contradictory lack-of-guidance
opening, while preserving the requirement to inspect actual documents. The
current source-bound evidence test also asserts that complete-reply property.
Date uncertainty, school-record obstacles, conditional application links,
self-funding, own-account transfer explanation, non-guarantee and pause/pacing
behaviour passed the exposed development checks.

The strict current-report test references v11, without weakening source-manifest,
scenario-contract, raw-response, call-count or workflow-configuration checks.
The consultant, income-routing and sponsor-replay tests passed 61 tests in 2.13s
before the additional saved-reply assertion. This remains a development set with
deterministic message rendering, not blind human-rated conversational quality.

Financial provider freshness still needs its own actual run; no complete fresh
all-green suite/CI is claimed. Real Gmail processing consent and recipient-side
final delivery remain outstanding. No worker restart or real-customer action
occurred during this checkpoint.
