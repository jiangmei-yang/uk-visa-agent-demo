# Source-bound provider refresh after sponsor conflict recovery

Both runs bind to `a2e92759a6afceaa195f17a77fdfbe0a8ae7a5b7`.
Original reports were retained, not edited into passing results.

- Consultant v12: 22 actual DeepSeek extraction calls, 22 passing turns,
  124,382 reported tokens, zero retries, mailbox calls or real documents.
  All 22 complete replies were read. These are the same four exposed development
  journeys, not a blind study. Rendering remains deterministic, not model-authored.
- Financial v20: four actual DeepSeek extraction calls, four passing results,
  8,744 reported tokens. All four frozen PDFs existed and were verified before
  calls; no PDF was authored or changed. Refreshed single-page previews were all
  viewed. Raw financial proposals were compared against visible subject, amount,
  currency, date, period, basis and account reference. Applicant and sponsor funds
  stayed separate. The receipt records the actual report hash and 101-file source
  manifest. No mailbox or real-document processing occurred.

Both previous freshness assertions now point to new reports with the same strict
source, schema, request and scenario checks. This is evidence freshness, not a
relaxed definition of accuracy. It does not establish full sponsor-pack recovery,
live Gmail recipient delivery, native installation or perfect naturalness.

Editorial observation retained: student journey turn 5 requests only one official
entry, but the response also explains the application sequence. It passes the
existing tests yet is more verbose than requested. This remains a quality gap;
the passing count is not presented as a full conversational score.

Verification completed after the reference update: the strict provider/consultant
group passed 55 tests in 2.01s; full lint and typing (102 source files) passed.
The full unfiltered suite then passed **5,395 tests**, zero failures, one Starlette
deprecation warning, in **103.34 seconds**. No tests were deselected or skipped.

A separate read-only check found the existing Gmail PID 69511 alive, nine SENT
records, one unknown processing-consent record and zero deliveries. The worker
was not reloaded with this candidate; this observation is neither current-code
Gmail verification nor recipient-side delivery evidence.
