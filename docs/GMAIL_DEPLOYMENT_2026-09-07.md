# Gmail 部署与实测记录

## 验收结论

**真实 Gmail 链路可运行，但本轮全自动材料交付未通过。** 接收、回信、附件入库、银行纠错、档案更新、旅行史登记及客户资料确认均已实际执行；没有生成或发送本轮最终 ZIP，不能将此运行称为端到端交付成功。

最后的确定性阻塞是 `application_record_intake_release_checked = false`：现有实现要求操作人员复核旅行史记录，且复核后重新取得客户确认。没有代填复核决定，也没有设置虚假的人工审核状态。含糊的“仍未完成的检查”和已定日期仍提到“未确定日期”也是待改进的客户回复。

本轮只更新本机 Gmail 后台。没有替换占用 8000 端口的既有 SSH 转发，也没有声称网页服务或 GitHub 远端已更新。

当前后台应用源码：`8ca6efb311a0f96ad451e038d16e33bded3d0484`。绑定该源码的对话评测 v26 为 22/22，资金文件评测 v32 为 4/4（9635 tokens）。它们分别是固定对话与文件测试，不能证明任意来信的全自动交付。

最终全量回归：**5673 passed，1 warning，118.63 秒**。未排除测试文件；唯一警告为 Starlette/httpx 依赖弃用。真实邮箱暴露出的操作人员复核依赖仍存在，测试全绿不改变上述未通过结论。

## 部署

- 应用源码：`cc57bb40eeb3e6fb5d80ac8dd7ce33d6ea0c5cfc`；评测提交 `7f8fd02`。
- 2026-09-07 09:13 UTC 更新唯一的 `com.visa-agent.gmail-user` 服务；新 PID 55143 已完成收件周期并回到 idle。
- 保留所有发件人接入、30 秒轮询和既有客户数据，不重新处理切换点前的邮件。
- 完整本地回归：5637 passed，1 个 Starlette 依赖弃用警告，116.72 秒。
- 当前源码对话评测 v22：22/22；资金文件评测 v28：4/4。都不是实际收件证明。

## 当前真实 Gmail 流程

- 通过 Gmail 网页从已授权演示邮箱发出普通主题咨询，建立独立案件 `case-3d9ff7d43f97`。
- 第一封顾问回复已在收件网页确认，无同意代码步骤，明确允许日期暂缓。
- 附件前完成案件级身份样本登记；没有修改旧案件、客户确认或问题状态。
- 第二轮补充已处理：旅游目的、点号生日、个人生活津贴均记入档案；发件端记录 SENT，收件网页仍待核实。

## 实测暴露的缺陷

- 首轮“英国旅游签证、去伦敦玩”没有提取旅游目的，导致回复仍说需要确认目的。
- 中文准备框架中出现 China / Hong Kong 混排，正文折行过碎。

## 实际补件阶段与修复

- 五份 PDF 已经通过 Gmail 网页发送并被服务接收。银行姓名 Lin Chan 与护照 Lin Chen 的冲突被正确发现。
- 已发送同账户、同期间、同余额的更正版。修复自然语言“附件是更正版，请用它替换”的解析后，旧银行文件已归档，新文件成为当前证据。
- 在读证明曾因没有承担旅行费用被误判。修复文件角色提示后，以保留文件的相同哈希进行有审计记录的重新读取；没有人工直接批准文件。
- 修复中英混合住址及“两年”居住时长的证据匹配。实际补充邮件已更新档案，日期和生日未被再次索取。
- 旅行史邮件触发误拦截：`我在英国没有亲属，也没有联系人` 未匹配联系人缺省声明。提交 `6e5d0ac` 修复；147 项针对性测试通过，包含假设、他人陈述及相反事实的反例。
- 09:46 UTC 服务更新为 PID 62191。仍然只运行一个接收服务，保留所有发件人接入。

- 第二次旅行史处理发现模型把明确的空联系人列表标成 complete。以合成文本单次诊断复现后，提交 `d402736` 根据完整原文校正该分类；没有绕过台账冲突检查。
- 两次恢复都通过 `review_held.py retry` 处理真实收到的补充邮件，审计保留。没有改写失败事件、伪造确认或直接放行材料。
- 09:51 UTC 唯一服务更新为 PID 63525。09:52 资料摘要发出，09:54 在 Gmail 收件端逐项核对后，通过网页回复确认。
- 当前源码绑定的对话评测 v25：22/22；资金文件 v31：4/4、9626 tokens。完整本地回归继续进行中。

- 首次较长的自然确认未被识别。提交 `8ca6efb` 支持明确的“逐项核对、各项都准确”句式，并测试条件、他人、未核对、遗漏及更正反例。
- 09:58 UTC 唯一 Gmail 服务更新为 PID 65006；随后同样的自然表达在真实邮件中成功设置 `profile_confirmed = true`。
- 停止本轮发信时：案件 DRAFT、无 human_review_reason、客户资料已确认；确定性检查仅剩最终摘要确认与旅行史操作人员复核两项未通过。无最终 ZIP，未调用 send-reviewed。
- `d402736` 全量回归结果为 5664 passed、1 warning；它不是后续 `8ca6efb` 的最终回归。

这轮是暴露问题并逐项修复的真实调试运行，不是零干预验收。尚未完成最终材料确认、ZIP 实收和录屏，不能宣称完整流程已通过。v23–v25、资金 v29–v31 是中间源码版本的评测，不作为最终源码的绑定验收报告。
