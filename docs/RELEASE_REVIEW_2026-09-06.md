# 面试交付复验 — 2026-09-06

这是范围明确的 Demo 交付检查，不是 100% 准确率、自然度评分或公开服务验收。
中文操作入口与三项要求对照见 [SUBMISSION.md](../SUBMISSION.md)。

## 顾问回复

先逐封读真实模型探针产生的回复，再调整顺序和篇幅。个性化材料动作现在位于通用
申请流程之前；第一次询问需要什么材料时，先看到材料类别，再看到办理步骤。
不改变已发送主题标识，所以部署后不会仅因为换了措辞就重复推送整段建议。
不发链接的偏好、未定日期、已知事实、暂停和确认门槛仍独立生效。

修改前、后分别调用 7 次 DeepSeek（无重试），均通过当时的明确检查。修改后新增了
实际发送正文中的动作顺序检查，而不是把测试全绿直接当成自然度满分。
模型只负责结构化提取，真实 workflow、SQLite 重开、outbox 和审核式发送器照常执行；
最后的 Gmail 传输由捕获器代替，**没有发送真实邮件**。场景时钟冻结在 2026-09-04，
不是假称实时政策查询。两轮分别为 27,163 和 27,088 tokens，共 54,251。

- [修改前原始报告](../eval_output/consultant_value_provider_2026-09-06.json)
- [修改后原始报告](../eval_output/consultant_value_provider_2026-09-06-v2.json)

这两份报告保存源码哈希，不是盲测；后续修改仅涉及 PDF 提示和网页显示，不能追溯性
改写报告里的源码。这里也不声称后续每一个文件都与咨询探针当时完全相同。

## 财务材料：保留失败，不绕开规则

最初预览命令在 30 秒后超时，尚未建立模型 client，产生 0 次模型调用。
指定原有系统 fontconfig 后命令虽成功，图片却是空白；目视检查发现了这一点。
随后只为本次渲染提供临时字体配置，指向 macOS 的实际字体目录。四页重新渲染并查看，
虚构标识与所有文字完整可读，最终 PNG 与仓库原有预览一致；PDF 字节始终保持冻结。

v8 调用 4 次，为 3/4：账单 A 的金额和 subject_name 有来源，但 facts 缺 full_name。
现有校验正确要求人工复核，没有用财务观察偷偷补全客户身份。
新增失败重放测试保留这一安全行为，并把提取提示中的两类字段要求写清楚。
探针也改为检查失败时退出非零，不再让成功退出码掩盖 all_passed=false。

v9 为 4/4。之后网页邮件换行修复改变了源码组合，因此最终 v10 又绑定完整 79 个源文件
和探针、schema、prompt、请求配置、Git 提交及四份 PDF，单次各调用一遍，为 4/4。
本次 v8–v10 共 12 次、25,945 tokens；财务实验全部历史合计 35 次、73,495 tokens。
所有失败和过往结果均保留。四份开发样本不代表所有账单或真实材料准确率。

## 安装、界面和 ZIP

最后一次本地全套为 **4,471 passed、1 warning、89.13 秒**；Ruff、严格 Mypy
（79 个源文件）和 diff check 通过。警告是现有 Starlette/httpx 测试客户端弃用提示，
不是运行失败，也没有为交付临时隐藏警告。

使用 `git archive` 导出干净的已提交源码，在独立目录构建 Docker 镜像。
验收实例只绑定本机 18080，运行数据放在一次性 tmpfs，不挂载现有客户卷或密钥。
检查脚本拒绝已使用的 lab；第一次脚本错误被修正后，重建的是这个一次性实例，
没有重置日常 8000 端口的数据。

第一版脚本错误地要求客户 ZIP 包含内部审计 JSON；真实设计将审计留在后台。
现改为验证七项客户交付文件、支持材料、已确认档案一致性，以及内部审计未被发给客户。
最终 HTTP 检查通过 12 项：健康、空白 lab、乱序拒绝、初始证据阻断、重复点击不改变状态、
补件后等待确认、确认后放行、重复下载一致、CRC、内容完整性、审计隔离及档案一致性。
[原始 HTTP 报告](../eval_output/release_http_2026-09-06.json) 保留 ZIP 文件名和哈希。

浏览器实看发现顾问邮件换行被合并；按现有界面规范增加换行保留和长链接折行，
重新构建后再次截图确认，摘要与材料清单逐行显示。没有改动交付控制或重新设计界面。

## 不能隐去的剩余验收

Gmail 当前是单个注册发件人的监督服务，现有档案还没有当前处理范围的有效同意。
没有代替客户同意、补写真实邮件或把捕获发送冒充收件人观察。
真实同意后的普通材料全流程、未受指导的面试官试用、原生 Windows 双击体验、
WhatsApp 实际设备收发仍需独立验收。面试 Demo 可以呈现这些边界，不能称全面完美。
