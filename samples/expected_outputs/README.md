# Expected output

`visa_application_pack_demo.zip` and `demo_report.json` are generated from the committed synthetic
email fixtures. They let an evaluator inspect the deliverable without running Python first.

Regenerate them with `make demo`, then copy the resulting ZIP/report here only after tests and PDF
render checks pass. No real-person data may be added to this directory.
